파이썬에서 주석을 적기위해서는
위 셀렉박스에서 markdown 을 선택 후 쓰면 주석처럼 된다.

앞의 In [] : 의 유무로 판단한다

커서가 나오지않을때 y 를 누르면 code 입력칸으로 변하고
		m 을 누르면 markdown 입력칸으로 변한다.

셀 선택 후 b 를 누르면 바로 밑 셀 하나 추가된다.
셀 선택 후 a 를 누르면 바로 위 셀 하나 추가된다.

자동완성 단축키 : tab